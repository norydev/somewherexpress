<%= content_for(:title) do %>
  <%= @competition %> | <%= @form.user %>
<% end %>

<%= content_for(:description) do %>
  Traitement de l'inscription de <%= @form.user %> à <%= @competition %>. <%= t('welcome.index.intro') %>
<% end %>

<div class="container padded-mini">
  <div class="row">
    <div class="col-sm-8 col-sm-offset-2 col-lg-6 col-lg-offset-3">
      <div class="panel panel-default">
        <%= render 'process', subscription: @form, competition: @competition, user: @form.user %>
      </div>
    </div>
  </div>
</div>
